/*
 * The Corne shield references &pro_micro_i2c (for OLED) but the Adafruit
 * Feather nRF52840 is not Pro Micro-compatible and doesn't define that label.
 * Map it to i2c0 so the DTS parses. OLEDs are disabled via CONFIG_ZMK_DISPLAY=n.
 */
pro_micro_i2c: &i2c0 {};
